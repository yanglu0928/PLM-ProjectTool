# AI 自主执行与最小人工确认规则 V1.1

版本：V1.1；日期：2026-09-24。用户本轮明确授权持续执行至交付可使用的程序包，并选定 LIC-03-A03 方案 A。V1.0 保留历史；未被本补充修改的条款继续适用。本规则优先于 V1.0 与旧版总控指令中要求逐项 L3/Gate 人工确认的执行流程，但不把未验证事项变成已通过的事实。

2026-09-26 用户再次确认方案 A 和持续执行授权：按计划推进至可使用程序包；原方案不兼容时由 AI 自主分析并实施可验证的最小调整，不逐项等待同意；偏差及调整均记录并同步 GitHub。此次确认不改变下述安全边界，也不替代客观 Gate 验收。

## 持续执行目标

- 按已确认实施方案、WBS 和实际依赖顺序，连续完成开发、测试、集成、打包、安装与升级验证，直至交付可使用程序包；普通 WBS 或 Gate 之间不等待“继续”指令。
- 发现原方案不兼容、实现不可行或基线偏差时，自主分析证据与替代方案，选择可验证、范围最小且可恢复的方案执行。无须再次取得用户对该偏差的逐项同意；所有偏差必须在实施前建立 Change Request/决策记录，保留原冻结版本与修改理由、影响、迁移/回滚、测试，并同步到 GitHub。
- Gate 不因用户默认授权而自动判定 PASS。只有验收证据齐全才可关闭；缺项记录为未通过或替代控制并继续不受阻塞的工作。不能把“默认接受调整”解释为免除测试、UAT 或发行验收。

## 持续有效的安全边界

- 不向外部 AI/服务发送客户正文、密钥、密码或私钥，除非该轮有明确的数据范围与用途授权；不把 Secret/客户数据/运行日志提交 Git。
- 不代用户购买、签订商业授权、消耗额度重置或进行其他付款；不得 force push、覆盖未知远端改动或直接向 `main` 提交开发代码。
- 破坏性生产数据操作、无法恢复的迁移或无法提供的真实测试环境不凭空假定成功。先做可逆的本地/临时库验证；客观条件不具备时记录风险并继续独立任务。
- License 私钥只留开发者工作台，客户侧只能持公钥；未验证的测试密钥来源不得标记为生产方案。正式业务事实的客户确认仍由实际授权客户完成，AI 代决策不伪造客户签署。

## 记录与同步

每项调整保留：来源、冲突/证据、方案比较、所选方案、与原基线的差异、影响/迁移/回滚、验证结果和剩余风险。更新 `STATUS.md`、相应进度/ADR/API/Schema/版本说明；完成并验证后，先 fetch、检查远端及 Secret，再提交推送到正确分支。未完成任务不得标为 PASS。

## 本轮批准的方案 A

`LIC-03-A03` 先缩为一次性受控可信时间初态初始化，不把测试密钥当生产来源；跨平台 `SecretKeyProvider`、密钥备份恢复及完整装配留在 PLT-02/Release 安全设计与验证任务。该拆分为已批准的范围/时序偏差，单独记录并追踪，不宣称生产可信来源已可用。
