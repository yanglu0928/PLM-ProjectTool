# DOC-03-A04-A04-P04-P03-P02-A01：上传操作栅栏适配

- 日期：2026-09-26；结果：Windows 11 单机本地 OS 锁适配 PASS；尚未接入业务命令，物理清理继续 BLOCKED。
- 来源与变更：`CR-DOC-008`。私有数据根下 `.upload-gates` 使用规范 UUID 前缀映射至固定 256 个锁文件，跨进程以操作系统非阻塞文件锁互斥，句柄关闭/进程终止释放。锁文件只含内部标识，不含正文或 Secret；非目录、重解析点、非普通文件和硬链接拒绝。
- 验证：Windows 11/Python 3.13 新增 5 项测试，覆盖相同桶争用、不同桶并行、跨进程崩溃释放、异常释放、零 UUID/目录伪装/硬链接拒绝。后端全部 546 项无失败（2 项既有符号链接环境跳过）；开发 wheel PASS。未在 Windows Server 2025 或 Debian 13 验证。
- Migration：无。公开 API：无。新增依赖：无。版本：`0.1.0.dev0`。
- 风险/下一项：A02 必须让 Content、Commit、Abort 的状态预检、外部 I/O 和最终事务完整持有同一栅栏，并让 Windows 显式写组合失败关闭。仅 A01 不提供物理清理授权；旧进程停写/目标账户与 A03 的引用及文件身份复核仍待。
