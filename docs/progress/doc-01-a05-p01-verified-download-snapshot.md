# DOC-01-A05-P01 下载前验证快照

- 日期：2026-09-26；状态：内部 Storage Adapter PASS；受权下载 Service/HTTP 尚未开放。
- 输入：冻结 API-02 下载在受权后流式返回且不暴露 locator；DM-03 对文件缺失/Hash 不符失败关闭；决策 `DEC-20260926-128`。
- 实现：仅接受规范持久 locator、大小/Hash/100 MB 上限；复制到数据根下私有 `SpooledTemporaryFile`（内存阈值 1 MiB），复制时计算 SHA-256，前后核对源句柄与路径身份/大小/mtime/单链接状态。全量校验前不把任何字节交给调用方；失败关闭并清理快照。成功快照与源文件脱钩，调用方负责关闭。
- 验证：Windows 11/Python 3.13 后端 530 项无失败（2 项既有符号链接环境跳过）；存储单元测试含大于 1 MiB 快照、源文件后续变更不影响快照、错误 Hash/大小/上限/暂存文件拒绝；开发 wheel PASS。
- 无 Migration、新依赖或公开 API 变化。并发临时磁盘容量、授权/状态复核、完整性事件、响应中断清理和正式下载 HTTP 仍待；本项不声称文件下载可用。
